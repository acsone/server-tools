This module is a technical module that does nothing by itself.

This addon allows to create temporary actions that will be removed by a cron.
Temporary actions are identified by a field 'is_temporary' and removed by the cron every one hour.
The deletion cron is configurable.
